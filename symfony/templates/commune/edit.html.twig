{% extends 'admin/layout.html.twig' %}

{% block title %}Modifier la commune{% endblock %}

{% block body_content %}
    <h1>Modifier la commune</h1>

    {{ include('commune/_form.html.twig', {'button_label': 'Modifier'}) }}

    <div class="admin-show d-flex">
        <div>
            {{ include('commune/_delete_form.html.twig') }}
        </div>
        <div>
            <a class="btn btn-default" href="{{ path('commune_index') }}">< Retour à la liste</a>
        </div>
    </div>
{% endblock %}
