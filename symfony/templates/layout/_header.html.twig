<nav class="navbar navbar-expand-lg navbar-light" id="mainNav">
    <div class="container">
        <a class="navbar-brand js-scroll-trigger" href="{{ path('home', {'_locale': app.request.locale }) }}">
            Prorim
{#            <img src="{{ asset('build/images/logo.svg') }}" alt="Logo Prorim">#}
        </a>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item"  {% if 'home' starts with app.request.attributes.get('_route') %} class="active" {% endif %}><a class="nav-link" href="{{ path('home') }}">{{ 'home.nav.home'|trans }}</a></li>
                <li class="nav-item" {% if 'contact' starts with app.request.attributes.get('_route') %} class="active" {% endif %}><a class="nav-link" href="{{ path('contact') }}">{{ 'home.nav.contact'|trans }}</a></li>
                {% set otherLocale = app.request.locale != 'fr' ? 'fr' : 'ar' %}
                <li class="nav-item"><a class="nav-link js-scroll-trigger" href="{{ path('home.' ~ otherLocale, app.request.attributes.get('_route_params')|merge(app.request.query.all)) }}">
                        {{ app.request.locale != 'fr' ? 'Français' : 'Arabe' }}
                    </a>
                </li>
                {% if app.user and is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin') }}">{{ 'website.view.home.admin.title'|trans }}</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    {% if not app.user %}
                        <a class="nav-link" href="{{ path('app_login') }}">{{ 'website.view.home.auth.login'|trans }}</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_logout') }}">{{ 'website.view.home.auth.logout'|trans }}</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>

